import type { ModuleConfig } from "../../types";

export const config: ModuleConfig = {
  slug: "pedidos",
  nombre: "Pedidos",
  categoria: "Comercial / Ventas",
  descripcion: "Pedidos de clientes con sus líneas de productos y estado.",
  ruta: "/pedidos",
  origen: ["la-casa-de-las-nueces"],
};

export default config;
