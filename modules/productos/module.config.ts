import type { ModuleConfig } from "../../types";

export const config: ModuleConfig = {
  slug: "productos",
  nombre: "Productos",
  categoria: "Comercial / Ventas",
  descripcion: "Catálogo de productos que la pyme vende, con precios.",
  ruta: "/productos",
  origen: ["la-casa-de-las-nueces"],
};

export default config;
