import type { ModuleConfig } from "../../types";

export const config: ModuleConfig = {
  slug: "stock",
  nombre: "Stock",
  categoria: "Inventario y Auditoría",
  descripcion: "Conteos periódicos de inventario por ingrediente.",
  ruta: "/stock",
  origen: ["la-casa-de-las-nueces"],
};

export default config;
