import type { ModuleConfig } from "../../types";

export const config: ModuleConfig = {
  slug: "ingredientes",
  nombre: "Ingredientes",
  categoria: "Operaciones / Producción",
  descripcion: "Materias primas: unidad de medida, precio unitario y stock.",
  ruta: "/ingredientes",
  origen: ["la-casa-de-las-nueces"],
};

export default config;
