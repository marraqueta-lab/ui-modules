import type { ModuleConfig } from "../../types";

export const config: ModuleConfig = {
  slug: "clientes",
  nombre: "Clientes",
  categoria: "Comercial / Ventas",
  descripcion: "Ficha de clientes de la pyme: datos de contacto y notas.",
  ruta: "/clientes",
  origen: ["la-casa-de-las-nueces", "marraqueta-kit (Tomás)"],
};

export default config;
