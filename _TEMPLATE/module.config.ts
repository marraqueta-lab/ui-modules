import type { ModuleConfig } from "../types";

export const config: ModuleConfig = {
  slug: "_TEMPLATE",
  nombre: "Nombre visible",
  categoria: "Comercial / Ventas",
  descripcion: "Una línea: qué hace el módulo.",
  ruta: "/slug",
  origen: [],
};

export default config;
